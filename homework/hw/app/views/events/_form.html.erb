<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :happening_on %><br />
    <%= f.datetime_select :happening_on %>
  </div>
  <div class="field">
    <%= f.label :venue_id %><br />
    <%= f.collection_select :venue_id,
                            Venue.all(:order => 'name ASC'),
                            :id,
                            :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
